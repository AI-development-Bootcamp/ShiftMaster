# Shared workspace typically doesn't need environment variables
# This file is kept for consistency with other workspaces
# If you need to add shared configuration in the future, add it here
VITE_API_URL=http://localhost:3000/api/v1
