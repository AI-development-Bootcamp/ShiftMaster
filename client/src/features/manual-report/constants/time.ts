export const DAILY_QUOTA_HOURS = 9;

export const HALF_VACATION_CAP_HOURS = 4.5;

export const HOURS = Array.from({ length: 12 }, (_, i) => i + 1);

export const MINUTES = Array.from({ length: 60 }, (_, i) => i);

export const PERIODS: ('AM' | 'PM')[] = ['AM', 'PM'];

export const TIME_PICKER_ITEM_HEIGHT = 40;
