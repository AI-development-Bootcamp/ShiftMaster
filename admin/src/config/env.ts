/**
 * Simple environment configuration for admin
 */

export const env = {
  apiUrl: import.meta.env.VITE_API_URL || '',
  renderDeployUrlAdmin: import.meta.env.VITE_RENDER_DEPLOY_URL_ADMIN || '',

};
